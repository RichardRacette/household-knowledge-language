name: Validate household records

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r requirements-research.txt

      - name: Run validation tests
        run: python -m unittest discover -v

      - name: Run all offline experiments
        run: python -m hkl_workbench run --output work/ci-run

      - name: Validate fictional examples
        run: |
          python validate_record.py ceramic_bird.json
          python validate_record.py composite_memory_shelf.json
          python validate_record.py annual_ornament_collection.json
